apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  # pushup-api-auth Secret is provisioned out-of-band via Terraform
  # (mattjewell.co.uk repo) and lives only in the cluster — not in git.
  # See README "Auth secrets (provisioned externally)".
  - ollama-secret.yaml
  - ingress.yaml
  - argocd-app.yaml
