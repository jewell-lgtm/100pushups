apiVersion: apps/v1
kind: Deployment
metadata:
  name: pushup-api
  namespace: pushups
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pushup-api
  template:
    metadata:
      labels:
        app: pushup-api
    spec:
      containers:
      - name: pushup-api
        image: registry.registry.svc.cluster.local:5000/pushup-api:latest
        ports:
        - containerPort: 3000
        env:
        - name: PORT
          value: "3000"
        - name: DATA_DIR
          value: "/data"
        # Phase 1.5.6: backend talks to Ollama over OrbStack's internal
        # network (same box). The previous LAN IP worked but exposed the
        # hop to anything on the LAN. Use host.docker.internal as a safe
        # universal default; override to an OrbStack `.local` hostname
        # (e.g. http://ollama.orb.local:11434) once verified reachable
        # from inside the pod with `kubectl exec ... curl $OLLAMA_URL/api/tags`.
        - name: OLLAMA_URL
          value: "http://host.docker.internal:11434"
        - name: OLLAMA_MODEL
          value: "llama3.2:3b"
        - name: NODE_ENV
          value: "production"
        envFrom:
        # AUTH_SECRET, REGISTER_API_KEY (Phase 1.5)
        - secretRef:
            name: pushup-api-auth
        # OLLAMA_USER, OLLAMA_PASSWORD (Phase 1.5.6 — backend → Ollama)
        - secretRef:
            name: pushup-api-ollama
            optional: true
        resources:
          requests:
            memory: 64Mi
            cpu: 50m
          limits:
            memory: 256Mi
            cpu: 250m
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: pushup-api-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pushup-api-data
  namespace: pushups
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
